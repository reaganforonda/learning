import axios from 'axios';

const INITIAL_STATE = {

}


export default function attendanceReducer(state=INITIAL_STATE, action) {
    switch(action.type) {

        default:
            return state
    }
}